{
  "buildCommand": "cd apps/web && npx prisma generate && npx next build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "npm install",
  "framework": "nextjs",
  "_cronNote": "Once-daily cron (Hobby plan limit). Premium tier upgrade path: multiple scans per day. Future: per-user timezone staggering. See pricing track in Notion.",
  "crons": [
    {
      "path": "/api/cron/scan-all",
      "schedule": "30 11 * * *"
    }
  ]
}
